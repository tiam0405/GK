spring.application.name=gk_bk
server.port=8081
spring.main.web-application-type=servlet

# === DataSource (MySQL) ===
spring.datasource.url=jdbc:mysql://localhost:3306/board?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=skan1020!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === MyBatis ===
mybatis.mapper-locations=classpath:/mappers/**/*.xml
mybatis.type-aliases-package=com.gk_bk.domain
mybatis.configuration.map-underscore-to-camel-case=true

# === SQL init ===
spring.sql.init.mode=always

# === Logging (optional) ===
logging.level.org.mybatis=INFO
logging.level.jdbc.sqlonly=INFO
logging.level.com.gk_bk=INFO

# === springdoc (Swagger UI) ===
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui
